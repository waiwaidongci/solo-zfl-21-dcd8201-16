{
  "name": "clock-escapement-tuning-api",
  "version": "2.0.0",
  "private": true,
  "description": "机械钟表擒纵调校 + 维修保险定损闭环（零依赖）",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=20"
  }
}
